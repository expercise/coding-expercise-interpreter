package com.expercise.interpreterapi.docker;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.runners.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class DockerServiceTest {

    @InjectMocks
    private DockerService dockerService;

    @Before
    public void init() {
//        dockerService.init();
    }

    @Test
    public void shouldRunDocker() {
//        InterpretResponse interpretResponse = dockerService.runContainer();
//        System.out.println("stdout:'" + interpretResponse.getStdOut() + "'");
//        System.out.println("stderr:'" + interpretResponse.getStdErr() + "'");
    }
}